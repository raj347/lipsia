PROJECT(vecm)

ADD_EXECUTABLE(vecm vecm.c)
TARGET_LINK_LIBRARIES(vecm ${VIAIO_LIBRARY}  ${LIPSIA_LIBRARY}
    ${GSL_GSL_LIBRARY} ${GSL_CBLAS_LIBRARY} )


INSTALL(TARGETS vecm
        RUNTIME DESTINATION bin
        COMPONENT RuntimeLibraries)
